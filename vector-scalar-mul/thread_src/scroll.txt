// Макроопределение констант
define {
    N = 10; // число потоков
    N_FD = 10; // число фрагментов данных
    VECTOR_SIZE = 1000;
}

// Описание исполнения
execution {
    // Основной вариант
    run {
        initVector1[0..N_DF - 1],
        initVector2[0..N_DF - 1],
        scalarVectors[0..N_DF - 1]
    } on ranks $ALL_RANKS;

    reduce printScalar[0..N_DF - 1] on ranks $ALL_RANKS;

    sequential run {
        printScalar[0] 
    } on rank $ROOT_RANK;
}